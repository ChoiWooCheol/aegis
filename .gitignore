# ==========================================
# 1. 최상단의 모든 파일과 폴더를 전면 차단합니다.
# (모델, 캐시, 파이썬 코드 등 깃허브 업로드 원천 봉쇄)
# ==========================================
/*

# ==========================================
# 2. Vercel 배포를 위한 프론트엔드 폴더와 필수 파일만 예외(!) 허용
# ==========================================
!/aegis-app/
!/.gitignore

# (선택) 데이터 빌더 스크립트 등 깃허브에 백업하고 싶은 파일이 있다면 아래처럼 추가
# !/build_web_data.py
# !/deploy.sh

# ==========================================
# 3. aegis-app 내부에 허용되었더라도, 무거운 프론트엔드 찌꺼기 파일은 다시 차단
# ==========================================
/aegis-app/node_modules/
# /aegis-app/dist/
# /aegis-app/build/
# /aegis-app/.next/
# /aegis-app/out/

# 보안 및 시스템 파일 차단
# /aegis-app/.env
# /aegis-app/.env.*
# /aegis-app/.DS_Store
# *.log